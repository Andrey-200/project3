generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // Строку url УДАЛИЛИ! Она теперь в prisma.config.ts
}

model User {
  id        Int      @id @default(autoincrement())
  login     String   @unique
  password  String
}